type StrapiEntity<T> = T extends "api::variation.variation"
  ? {
      id: number;
      variationName: string;
      variationValue: string;
      products: StrapiEntity<"api::example-product.example-product">[];
    }
  : T extends "api::example-product.example-product"
  ? {
      id: number;
      name: string;
      description: string;
      images: StrapiMedia[];
      variations: StrapiEntity<"api::variation.variation">[];
    }
  : never;

type StrapiMedia = {
  id: number;
  url: string;
  ext: string;
  mime: string;
  size: number;
  previewUrl: string | null;
  provider: string;
  provider_metadata: {
    public_id: string;
    resource_type: string;
  } | null;
};

type Entity = StrapiEntity<
  "api::example-product.example-product" | "api::variation.variation"
>;