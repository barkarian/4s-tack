The transformed types for the given json entities are:

Type for "api::example-product.example-product":
```typescript
type StrapiEntity<T> = T extends "api::example-product.example-product"
  ? {
      id: number;
      name: string;
      description: string;
      images: StrapiEntity<"api::upload_file.upload_file">[];
      variations: StrapiEntity<"api::variation.variation">[];
    }
  : never;
```

Type for "api::variation.variation":
```typescript
type StrapiEntity<T> = T extends "api::variation.variation"
  ? {
      id: number;
      variationName: string;
      variationValue: string;
      products: StrapiEntity<"api::example-product.example-product">[];
    }
  : never;
```

Note: The type "StrapiEntity<"api::upload_file.upload_file">" is used for the `images` attribute of the "api::example-product.example-product" type and is not provided in the given data.